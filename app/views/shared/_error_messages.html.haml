- if @user.errors.any?
  .panel.panel-danger#error_explanation
    .panel-heading
      %h3.panel-title The form contains #{pluralize(@user.errors.count, "error")}.
    .panel-body
      %ul
        - @user.errors.full_messages.each do |msg|
          %li= msg
